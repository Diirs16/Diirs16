package com.petconnect.app.ui.theme

import androidx.compose.ui.graphics.Color

// =========================
// Paleta principal de marca
// =========================

// Azul principal (títulos, links, identidad)
val BluePrimary = Color(0xFF1565C0)

// Azul claro para superficies suaves (cards/contendores)
val BlueLight = Color(0xFFE3F2FD)

// Naranja principal para botones/acciones
val OrangePrimary = Color(0xFFF57C00)

// Naranja claro para estados o acentos secundarios
val OrangeLight = Color(0xFFFFE0B2)

// Texto oscuro principal
val TextDark = Color(0xFF1A1A1A)

// Fondo general claro
val BackgroundSoft = Color(0xFFF7F9FC)
